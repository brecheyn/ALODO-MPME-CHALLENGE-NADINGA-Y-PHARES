{
  "installCommand": "npm install --prefix metric-frontend",
  "buildCommand": "npm run build --prefix metric-frontend",
  "outputDirectory": "metric-frontend/dist/metric-frontend/browser",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
